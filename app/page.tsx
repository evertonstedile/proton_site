import { Hero } from "@/components/home/Hero";

/**
 * Home — narrativa cinematográfica (Fase 3, seção por seção).
 * Atual: HERO (Checkpoint 2). Próximas seções (manifesto, obras, números,
 * processo, serviços, depoimentos, CTA) entram após validação.
 */
export default function Home() {
  return (
    <main>
      <Hero />
    </main>
  );
}
